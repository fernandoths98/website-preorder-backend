import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { DataSource, Repository } from 'typeorm'; import slugify from 'slugify'; import { Product, MerchantProductStatus } from '../products/entities/product.entity'; import { ProductImage } from '../products/entities/product-image.entity'; import { SubmitMerchantProductDto } from './dto/submit-merchant-product.dto';
@Injectable() export class MerchantProductsService { constructor(@InjectRepository(Product) private products:Repository<Product>,@InjectRepository(ProductImage) private images:Repository<ProductImage>,private ds:DataSource){}
 list(merchantId:string){return this.products.find({where:{merchantId},relations:{images:true},order:{createdAt:'DESC'}})}
 async submit(merchantId:string,dto:SubmitMerchantProductDto){return this.ds.transaction(async em=>{const slugBase=slugify(dto.name,{lower:true,strict:true})||'produk';let slug=`${slugBase}-m${merchantId}`,n=1;while(await em.findOne(Product,{where:{slug},withDeleted:true}))slug=`${slugBase}-m${merchantId}-${++n}`;const sku=`M${merchantId}-${Date.now().toString(36).toUpperCase()}`;const p=await em.save(Product,em.create(Product,{merchantId,merchantStatus:MerchantProductStatus.PENDING,sku,slug,name:dto.name,description:dto.description??null,category:dto.category,unit:dto.unit,basePrice:dto.basePrice,margin:dto.margin,isActive:false,imageUrl:dto.imageUrls[0]}));await em.save(ProductImage,dto.imageUrls.map((url,i)=>em.create(ProductImage,{productId:p.id,imageUrl:url,sortOrder:i,isPrimary:i===0})));return em.findOneOrFail(Product,{where:{id:p.id},relations:{images:true}})})}
 async review(id:string,status:'approved'|'rejected',note?:string){const p=await this.products.findOne({where:{id}});if(!p||!p.merchantId)throw new NotFoundException('Produk mitra tidak ditemukan');p.merchantStatus=status=== 'approved'?MerchantProductStatus.APPROVED:MerchantProductStatus.REJECTED;p.merchantReviewNote=note?.trim()||null;p.isActive=status==='approved';return this.products.save(p)}
}